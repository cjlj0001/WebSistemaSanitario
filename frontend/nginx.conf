server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Google Identity Services uses a cross-origin popup and postMessage.
  add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
